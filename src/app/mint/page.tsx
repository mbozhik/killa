export default function MintPage() {
  const mobileHeight = '!h-[100svh] h-[100vh]'
  return (
    <section className={`grid place-items-center w-screen ${mobileHeight}`}>
      <h1 className="text-2xl tracking-tighter animate-pulse">Mint</h1>
    </section>
  )
}
